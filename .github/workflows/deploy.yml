name: deploy

on:
  push:
    branches:
      - 'dev'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: (Release) Get current date
        id: date
        run: echo "::set-output name=date::$(date + '%Y/%m/%d')"

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew
        shell: bash

      - name: Build with Gradle
        run: |
          ./gradlew -version
          ./gradlew clean build -x test
        shell: bash

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}


      - name : Build Docker Image & Push to Docker Hub
        run: |
          docker build . -t ${{ secrets.DOCKERHUB_USERNAME }}/spring-boot-tutorial-18th
          docker build ./nginx -t twoosky/docker-nginx
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/spring-boot-tutorial-18th
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/nginx