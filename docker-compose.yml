version: "3.8"
services:
  backend:
    image: "yourzinc/spring-boot-tutorial-18th"
    ports:
      - "8080:8080"
    restart: "always"

  nginx:
    image: "yourzinc/nginx"
    ports:
      - "80:80"
    restart: "always"
    depends_on:
      - "backend"